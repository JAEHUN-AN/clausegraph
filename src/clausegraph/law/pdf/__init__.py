"""표준약관 PDF 레이아웃 파서.

기존 경로(행정규칙 XML의 `<별표내용>`)는 조판 결과를 **평문으로 눌러** 준다.
표는 괘선 문자로 그려지고, 쪽 번호는 아예 없다. 이 패키지는 같은 문서의
PDF 조판본을 좌표 단위로 읽어 세 가지를 얻는다.

1. **쪽 번호** — 근거 조항을 "제5조(203쪽)"로 인용할 수 있다. 평문에는 없다.
2. **표의 셀 경계** — 괘선 문자 대신 실제 사각형으로 읽는다.
3. **독립 원천** — 같은 문서를 다른 조판 경로로 파싱해 서로 채점한다.

3번이 이 패키지를 만든 이유다. 지금까지 조문 파서의 정확도는 수동 표본으로만
확인했다. 같은 문서를 두 경로로 파싱하면 **불일치가 곧 둘 중 하나의 결함**이라
표본 없이 전수 채점이 된다.
"""
